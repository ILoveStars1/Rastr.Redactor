class figure:
    def _init_(self):
        self.line_start = None
    def Line():
        origin = (event.x, event.y)
        if not self.line_start:
            self.line_start= (x,y)
        else:
            x_origin, y_origin = self.line_start
            self.line_start = None
            canvas.create_line = (x_origin, y_origin, x, y)
            
        
        
    def Circle():
        origin = (x1, y1)

    def Rectangle():
        origin = (x1, y1)
